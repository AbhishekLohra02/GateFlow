# Evidence

The AWS account backing this project closes on **2026-09-27** and everything
in it is deleted. This repository is the portfolio; the account is temporary
scaffolding.

So: capture proof here **as each step happens**, not afterwards. Terraform
plan/apply output, CI run links, console screenshots, smoke-test results.

Naming: `NN-short-description.{md,png,txt}` in step order.
